---
title: Table
description: Options for styling tabular data.
---

import { graphql } from 'gatsby';
import { ComponentHeader, ComponentFooter } from 'components/thumbprint-components';
import Alert from 'components/alert';

<ComponentHeader data={props.data} />

<Alert title="This component is moving out of Thumbprint" type="warning">
    This component{' '}
    <a href="https://github.com/thumbtack/thumbprint/issues/544">
        will soon be moved out of Thumbprint
    </a>{' '}
    and into the website codebase. No action is required of you and you may continue using it. The
    Design Systems team will own the migration.
</Alert>

## Table options

### Default

```html
<table class="tp-table">
    <thead>
        <tr>
            <th>Professional</th>
            <th class="tp-text--right">Numbers</th>
            <th class="tp-cell-collapse">Status</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Professional name one</td>
            <td class="tp-text--right">258</td>
            <td>Up</td>
        </tr>
        <tr>
            <td>Professional name two that runs longer</td>
            <td class="tp-text--right">19</td>
            <td>Down</td>
        </tr>
    </tbody>
</table>
```

### Rules

```html
<table class="tp-table tp-table--rule">
    <thead>
        <tr>
            <th>Quote</th>
            <th class="tp-text--right">Numbers</th>
            <th class="tp-cell-collapse">Status</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Quote title one</td>
            <td class="tp-text--right">258</td>
            <td>Up</td>
        </tr>
        <tr>
            <td>Quote title two that runs longer</td>
            <td class="tp-text--right">19</td>
            <td>Down</td>
        </tr>
        <tr>
            <td>Quote title three</td>
            <td class="tp-text--right">400</td>
            <td>Up</td>
        </tr>
    </tbody>
</table>
```

### Bottom border

```html
<table class="tp-table tp-table--rule-bottom">
    <thead>
        <tr>
            <th>Quote</th>
            <th class="tp-text--right">Numbers</th>
            <th class="tp-cell-collapse">Status</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Quote title one</td>
            <td class="tp-text--right">258</td>
            <td>Up</td>
        </tr>
        <tr>
            <td>Quote title two that runs longer</td>
            <td class="tp-text--right">19</td>
            <td>Down</td>
        </tr>
        <tr>
            <td>Quote title three</td>
            <td class="tp-text--right">400</td>
            <td>Up</td>
        </tr>
    </tbody>
</table>
```

<ComponentFooter data={props.data} />

export const pageQuery = graphql`
    {
        # Get links to by path to display in the navbar.
        platformNav: allSitePage(filter: { path: { glob: "/components/table/*/" } }) {
            edges {
                node {
                    ...PlatformNavFragment
                }
            }
        }
        # Get package information by NPM package name.
        packageTable: thumbprintComponent(name: { eq: "@thumbtack/thumbprint-scss" }) {
            ...PackageTableFragment
        }
    }
`;
