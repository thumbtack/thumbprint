---
title: Upsell
description: Box for content.
---

import { graphql } from 'gatsby';
import Alert from 'components/alert';
import { InlineCode } from 'components/mdx';
import { ComponentHeader, ComponentFooter } from 'components/thumbprint-components';
import APITable from 'components/api-table';

<ComponentHeader data={props.data} />

Images

### Default

```email
<callout class="flush-bottom type-primary">
    <h1 class="text-center">Get the app.</h1>
    <p class="text-center">It's free. And it's how busy pros stay busy.</p>
    {{> button
        href="https://thumbtack.com"
        text="Get the app"
        align="center"
    }}
    <spacer size="8" />
    {{> image
        src="https://staging-email-preview.thumbtack.io/img/illustrations/Phone.png"
        width="368"
        align="center"
    }}
</callout>
```

## Props

<APITable
    properties={[
        {
            name: 'href',
            description: 'URL of the button destination.',
            type: {
                name: 'string',
            },
            isRequired: true,
        },
        {
            name: 'text',
            description: 'Text content of the button.',
            type: {
                name: 'string',
            },
            isRequired: true,
        },
    ]}
/>

<ComponentFooter data={props.data} />

export const pageQuery = graphql`
    {
        # Get links to by path to display in the navbar.
        platformNav: allSitePage(filter: { path: { glob: "/components/upsell/*/" } }) {
            edges {
                node {
                    ...PlatformNavFragment
                }
            }
        }
        # Get package information by NPM package name.
        packageTable: thumbprintComponent(name: { eq: "@thumbtack/thumbprint-email" }) {
            ...PackageTableFragment
        }
    }
`;
