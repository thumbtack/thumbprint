---
title: Select
description: Dropdown for selecting an item from a larger set.
---

import { graphql } from 'gatsby';
import { ComponentHeader, ComponentFooter } from 'components/thumbprint-components';

<ComponentHeader data={props.data} />

## Select dropdown with state management

`Select` is a controlled component. This means that the selected `option`, if any, must be provided as the `value` prop. The `onChange` function updates the `value` when the user selects a new option.

In this example, notice how `value` is stored within `this.state`. The `onChange` function updates the state when the user selects a new option.

```jsx
class SelelctExample extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            value: 'ca',
        };

        this.onChange = this.onChange.bind(this);
    }

    onChange(value) {
        this.setState({
            value,
        });
    }

    render() {
        return (
            <div>
                <Label for="example-93871">Select a state</Label>
                <Select
                    id="example-93871"
                    size="large"
                    value={this.state.value}
                    onChange={this.onChange}
                >
                    <option value="ny">New York</option>
                    <option value="ca">California</option>
                    <option value="tn">Tennessee</option>
                    <option value="fl">Florida</option>
                </Select>
            </div>
        );
    }
}
```

## Select dropdown sizes

### Large

This is the default size for `Select` dropdowns.

```jsx
class SelelctExample extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            value: 'ca',
        };

        this.onChange = this.onChange.bind(this);
    }

    onChange(value) {
        this.setState({
            value,
        });
    }

    render() {
        return (
            <div>
                <Label for="example-10490">Select a state</Label>
                <Select
                    id="example-93871"
                    size="large"
                    value={this.state.value}
                    onChange={this.onChange}
                >
                    <option value="ny">New York</option>
                    <option value="ca">California</option>
                    <option value="tn">Tennessee</option>
                    <option value="fl">Florida</option>
                </Select>
            </div>
        );
    }
}
```

### Small

```jsx
class SelelctExample extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            value: 'ca',
        };

        this.onChange = this.onChange.bind(this);
    }

    onChange(value) {
        this.setState({
            value,
        });
    }

    render() {
        return (
            <div>
                <Label for="example-38189">Select a state</Label>
                <Select
                    id="example-38189"
                    size="small"
                    value={this.state.value}
                    onChange={this.onChange}
                >
                    <option value="ny">New York</option>
                    <option value="ca">California</option>
                    <option value="tn">Tennessee</option>
                    <option value="fl">Florida</option>
                </Select>
            </div>
        );
    }
}
```

## Widths

By default, the `Select` component renders as an `inline-block` element. Its width is determined by the `option` with the longest text.

The `isFullWidth` prop can be used to set the width to 100%.

### Full width

```jsx
class SelelctExample extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            value: 'ca',
        };

        this.onChange = this.onChange.bind(this);
    }

    onChange(value) {
        this.setState({
            value,
        });
    }

    render() {
        return (
            <div>
                <Label for="example-39018">Select a state</Label>
                <Select
                    id="example-39018"
                    isFullWidth
                    value={this.state.value}
                    onChange={this.onChange}
                >
                    <option value="ny">New York</option>
                    <option value="ca">California</option>
                    <option value="tn">Tennessee</option>
                    <option value="fl">Florida</option>
                </Select>
            </div>
        );
    }
}
```

## Disabled selects

The `isDisabled` prop visually and functionally disables the `Select`.

```jsx
class SelelctExample extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            value: 'ca',
        };

        this.onChange = this.onChange.bind(this);
    }

    onChange(value) {
        this.setState({
            value,
        });
    }

    render() {
        return (
            <div>
                <Label for="example-91908">Select a state</Label>
                <Select
                    id="example-91908"
                    isDisabled
                    value={this.state.value}
                    onChange={this.onChange}
                >
                    <option value="ny">New York</option>
                    <option value="ca">California</option>
                    <option value="tn">Tennessee</option>
                    <option value="fl">Florida</option>
                </Select>
            </div>
        );
    }
}
```

## Select dropdown with an error

The `hasError` prop can be used to visually represent an error.

This prop only changes the select dropdown’s color. It should be used alongside an error message that helps users advance through the form.

```jsx
class SelelctExample extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            value: 'ca',
        };

        this.onChange = this.onChange.bind(this);
    }

    onChange(value) {
        this.setState({
            value,
        });
    }

    render() {
        return (
            <div>
                <Label for="example-39108">Select a state</Label>
                <Select
                    id="example-39108"
                    hasError
                    value={this.state.value}
                    onChange={this.onChange}
                >
                    <option value="ny">New York</option>
                    <option value="ca">California</option>
                    <option value="tn">Tennessee</option>
                    <option value="fl">Florida</option>
                </Select>
            </div>
        );
    }
}
```

<ComponentFooter data={props.data} />

export const pageQuery = graphql`
    {
        # Get links to by path to display in the navbar.
        platformNav: allSitePage(filter: { path: { glob: "/components/select/*/" } }) {
            edges {
                node {
                    ...PlatformNavFragment
                }
            }
        }
        # Get package information by NPM package name.
        packageTable: thumbprintComponent(name: { eq: "@thumbtack/thumbprint-react" }) {
            ...PackageTableFragment
        }
        # Get component props by path to component file.
        reactComponentProps: file(
            relativePath: { eq: "thumbprint-react/components/Select/index.jsx" }
        ) {
            ...ReactComponentPropsFragment
        }
    }
`;
