import UIKit.UIColor

{{#eachSectionWithPlatformTokens this}}
{{#if description}}
/// {{description}}
{{/if}}
public enum {{formatSectionName this}} {
    {{#each tokens as |token|}}
    {{#if token.value.ios}}
    {{#if token.description}}
    /// {{token.description}}
    {{/if}}
    {{#ifEquals token.type "color"}}
    /// Maps to {{token.value.web}}
    {{/ifEquals}}
    {{#if token.deprecated}}
    @available(*, deprecated)
    {{/if}}
    public static let {{formatId token}}: {{{formatValue token}}}
    {{/if}}
    {{/each}}
}
{{#unless @last}}

{{/unless}}
{{/eachSectionWithPlatformTokens}}
