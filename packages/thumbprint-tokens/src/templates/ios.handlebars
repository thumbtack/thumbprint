import UIKit.UIColor

{{#eachSectionWithPlatformTokens this}}
public struct {{formatSectionName this}} {
    {{#each tokens}}
    {{#if value.ios}}
    {{#if description}}
    /// {{description}}
    {{/if}}
    {{#if deprecated}}
    @available(*, deprecated)
    {{/if}}
    public static let {{formatId this}}: {{{formatValue this}}}
    {{/if}}
    {{/each}}
}
{{#unless @last}}

{{/unless}}
{{/eachSectionWithPlatformTokens}}
