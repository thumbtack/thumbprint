{{#each this as |section|}}
{{#each section.tokens as |token|}}
{{#if token.value.web}}
{{#if token.description.web}}
// {{token.description.web}}
{{/if}}
{{#if token.deprecated}}
// @deprecated
{{/if}}
{{formatId section token}}: {{{formatValue token.value}}};
{{/if}}
{{/each}}
{{/each}}
