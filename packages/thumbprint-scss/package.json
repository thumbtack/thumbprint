{
    "name": "@thumbtack/thumbprint-scss",
    "description": "SCSS components for building Thumbtack UI",
    "version": "0.0.0",
    "author": "Tom Genoni <tom@thumbtack.com>",
    "main": "dist/thumbprint-scss.css",
    "style": "dist/thumbprint-scss.css",
    "files": [
        "dist"
    ],
    "publishConfig": {
        "registry": "https://registry.npmjs.org/",
        "access": "public"
    },
    "license": "Apache-2.0",
    "scripts": {
        "start": "yarn clear && yarn generateCss",
        "clear": "rm -rf dist",
        "generateCss": "mkdir -p dist && node-sass -q src/thumbprint-scss.scss dist/thumbprint-scss.css && postcss dist/thumbprint-scss.css --map --use autoprefixer cssnano --replace",
        "prepublishOnly": "../../scripts/should-build-package.js -- yarn start"
    },
    "devDependencies": {
        "@thumbtack/thumbprint-tokens": "^6.2.4",
        "autoprefixer": "^9.0.1",
        "cssnano": "^4.1.0",
        "node-sass": "^4.9.2",
        "postcss-cli": "^6.1.1"
    }
}
