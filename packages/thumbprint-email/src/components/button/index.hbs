{{#equal align "center"}}
    <row>
        <columns>
            <center>
{{/equal}}

            <button href="{{href}}" class="
                    {{#unless preventExpanded}} small-expanded {{/unless}}
                    {{#equal type "secondary" }} secondary {{/equal}}
                    {{#if forceExpanded }} expanded {{/if}}
                ">
                {{{text}}}
            </button>

{{#equal align "center"}}
            </center>
        </columns>
    </row>
{{/equal}}