{{~#if backgroundImage ~}}
<div
    style="
        background-image:url({{~backgroundImage~}});
    "
    class="callout-bkg-img"
>
{{~/if~}}
    <callout
        {{#if height}}height="{{height}}"
        {{/if}} class="{{~#if type ~}} {{~type~}} {{~/if~}} {{~#if flush ~}} flush-{{~flush~}} {{~/if~}}"
    >
        {{> @partial-block }}
    </callout>
{{~#if backgroundImage ~}}
</div>
{{~/if~}}